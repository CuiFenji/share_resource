{
  "name": "share-resource",
  "version": "1.0.0",
  "description": "极锐光影资源分享站",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp *.html dist/ && cp -r css js images dist/ && echo 'OK'"
  }
}
